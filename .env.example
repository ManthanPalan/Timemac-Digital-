# Optional: override the domain used for the sitemap and social previews.
# Vercel's production domain is used automatically when this is unset.
# Include https:// and set this before building when using a custom domain.
# SITE_URL=https://www.your-domain.com
